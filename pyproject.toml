[project]
name = "patient-call-analysis"
version = "0.1.0"
description = "Patient call analysis agent built with LangChain Deep Agents"
requires-python = ">=3.11"
dependencies = [
    "deepagents",
    "langchain>=1.0,<2.0",
    "langchain-core>=1.0,<2.0",
    "langsmith>=0.8.18",
    "langchain-anthropic",
    "python-dotenv",
    "langgraph-cli[inmem]",
]
